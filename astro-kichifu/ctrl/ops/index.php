<?php
// ==========================================================================
// /ctrl/ops/ — 入口。CTRL のログイン状態を見て振り分けるだけ。
//   ログイン済み → ダッシュボードへ / 未ログイン → CTRL のログイン画面へ
//   （ops 独自ログインは廃止。CTRL に入っていれば追加のログインは不要）
// ==========================================================================
require_once __DIR__ . '/../_lib.php';

if (current_admin()) {
    redirect('/ctrl/ops/dashboard/');
}
redirect('/ctrl/login.php');
